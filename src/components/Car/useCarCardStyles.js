import { makeStyles } from '@material-ui/core';

export const useCarCardStyles = makeStyles({
  root: {
    maxWidth: 300,
  },
  cardTitle: {
    fontSize: '24px',
  },
  cardSubtitle: {
    fontSize: '15px',
    fontWeight: 'bold',
  },
  featureList: {
    fontSize: '15px',
  },
});
